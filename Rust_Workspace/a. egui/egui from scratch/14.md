1. test 1 tab. is working find when it snap and floating. seem like the tab inside a window. cannot snap back where it original position. when window is floating with the tab when I add a new tab it crash. 2. test 2 tabs when I click add from the menu the tab will appear to the right of the first tab. if I move to the top/right/bot/left it will show same ID, then once I want window to float It crash. 3. test 3 tabs. all 3 tabs in the same window, once I move one tab to the far right have the same ID error. the first and the second tab in the same floating winows. once I moved the second tab to the bottom. the first tab ID changed to different ID 6B477(add 7 at the edn) before that just 6B47. now the second tab and third tab has the same ID 6B47. lastly once I move one window out of order want it to be float, it crash. 
   
2. I am not sure we have correct understanding. I want floating window to combine to another floating window to have multiple tab inside the same floating window. I don't meant to have tab to do the same work/task. I still want each tab to be able to work it own task.