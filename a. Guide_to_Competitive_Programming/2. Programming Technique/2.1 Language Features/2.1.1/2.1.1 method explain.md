Below is a detailed explanation of all the methods (functions or traits) that appear in the provided Rust code for competitive programming input and output. These methods are part of the Rust standard library (`std::io` and related modules) and are used to handle various I/O operations efficiently and safely. I’ll break this down by context, explaining each method’s purpose, signature, and usage, tailored to the competitive programming context.

---

### General Context: `std::io` Module
The `std::io` module provides Rust's core input and output functionality. It includes types like `Stdin`, `Stdout`, and traits like `BufRead` and `Write`, which are heavily utilized in the code.

#### 1. **`io::stdin`**
- **Purpose**: Returns a handle to the standard input stream.
- **Signature**: `pub fn stdin() -> Stdin`
- **Explanation**: This static method creates a `Stdin` object, representing the input stream (typically the keyboard or redirected input file). It’s the entry point for reading from standard input.
- **Usage in Code**:
  ```rust
  let mut stdin = io::stdin().lock();
  ```
  - `lock()` is called immediately to acquire a locked handle (see below), ensuring efficient buffered reading.

#### 2. **`Stdin::lock`**
- **Purpose**: Locks the standard input stream and returns a locked reference implementing `BufRead`.
- **Signature**: `pub fn lock(&self) -> StdinLock<'_>`
- **Explanation**: `Stdin::lock` provides a `StdinLock`, which implements the `BufRead` trait. Locking prevents concurrent access and enables buffered reading, reducing system calls and improving performance—crucial for competitive programming with large inputs.
- **Usage in Code**:
  ```rust
  let mut stdin = io::stdin().lock();
  ```
  - The `mut` keyword allows modification of the internal buffer, and the result is often used with methods like `read_line`.

#### 3. **`BufRead::read_line`**
- **Purpose**: Reads a line of text into a string buffer until a newline (`\n`) or EOF is encountered.
- **Signature**: `fn read_line(&mut self, buf: &mut String) -> Result<usize, Error>`
- **Explanation**: This method, from the `BufRead` trait, appends data to the provided `String` buffer (`buf`) and returns the number of bytes read (as a `Result`). It includes the newline character in the buffer unless EOF is reached. The `expect` call unwraps the `Result`, assuming no I/O errors in a contest setting.
- **Usage in Code**:
  ```rust
  stdin.read_line(&mut input).expect("Failed to read line");
  ```
  - Reads into `input`, a mutable `String`, and panics with a custom message if the operation fails.

#### 4. **`String::trim`**
- **Purpose**: Removes leading and trailing whitespace from a string, including newlines.
- **Signature**: `pub fn trim(&self) -> &str`
- **Explanation**: This method returns a slice of the string with whitespace (spaces, tabs, newlines) removed from both ends. It’s useful after `read_line` to clean up input for parsing.
- **Usage in Code**:
  ```rust
  let mut iter = input.trim().split_whitespace();
  ```
  - Prepares the input string for tokenization by removing the trailing newline.

#### 5. **`str::split_whitespace`**
- **Purpose**: Splits a string into an iterator of substrings separated by whitespace.
- **Signature**: `pub fn split_whitespace(&self) -> SplitWhitespace<'_>`
- **Explanation**: This method returns an iterator over the string’s words, where words are sequences of non-whitespace characters separated by spaces, tabs, or newlines. It’s ideal for parsing space-separated numbers or strings in competitive programming input.
- **Usage in Code**:
  ```rust
  let mut iter = input.trim().split_whitespace();
  ```
  - Creates an iterator (`iter`) over tokens in the trimmed input.

#### 6. **`Iterator::next`**
- **Purpose**: Advances the iterator and returns the next item.
- **Signature**: `fn next(&mut self) -> Option<Self::Item>`
- **Explanation**: This method, from the `Iterator` trait, returns `Some(item)` for the next element or `None` if the iterator is exhausted. The `unwrap()` call assumes an item exists, which is valid for expected input formats.
- **Usage in Code**:
  ```rust
  let a: i32 = iter.next().unwrap().parse().expect("Expected an integer");
  ```
  - Retrieves the next token from `iter` for parsing.

#### 7. **`str::parse`**
- **Purpose**: Parses a string slice into a specified type (e.g., `i32`, `String`).
- **Signature**: `fn parse<F: FromStr>(&self) -> Result<F, F::Err>`
- **Explanation**: This method attempts to convert a string slice into a target type (e.g., `i32`) and returns a `Result`. The `expect` call unwraps the `Result`, panicking if parsing fails, which is acceptable in contests with guaranteed input.
- **Usage in Code**:
  ```rust
  let a: i32 = iter.next().unwrap().parse().expect("Expected an integer");
  ```
  - Converts the token to an `i32`, with error handling via `expect`.

#### 8. **`io::stdout`**
- **Purpose**: Returns a handle to the standard output stream.
- **Signature**: `pub fn stdout() -> Stdout`
- **Explanation**: This static method creates a `Stdout` object, representing the output stream (typically the console or redirected output file). It’s the starting point for writing to standard output.
- **Usage in Code**:
  ```rust
  let stdout = io::stdout();
  let mut stdout = BufWriter::new(stdout.lock());
  ```
  - Often paired with `lock()` and `BufWriter` for efficient output.

#### 9. **`Stdout::lock`**
- **Purpose**: Locks the standard output stream and returns a locked reference implementing `Write`.
- **Signature**: `pub fn lock(&self) -> StdoutLock<'_>`
- **Explanation**: `Stdout::lock` provides a `StdoutLock`, which implements the `Write` trait. Locking ensures thread-safe writing and is typically wrapped in `BufWriter` for buffered output, reducing system calls.
- **Usage in Code**:
  ```rust
  let mut stdout = io::stdout().lock();
  ```
  - The `mut` keyword allows writing, and the result is used with `write!` or `writeln!`.

#### 10. **`Write::write` (via `write!` macro)**
- **Purpose**: Writes a formatted string to the output stream without a newline.
- **Signature**: `fn write(&mut self, buf: &[u8]) -> Result<usize, Error>` (underlying trait method)
- **Explanation**: The `write!` macro leverages the `Write` trait to format and write data to the stream. It returns a `Result` indicating the number of bytes written. It’s used for custom output formatting without automatic newlines.
- **Usage in Code**:
  ```rust
  write!(stdout, "{} {} {}", a, b, x).expect("Failed to write");
  ```
  - Formats `a`, `b`, and `x` with spaces and writes them, with `expect` handling errors.

#### 11. **`Write::writeln` (via `writeln!` macro)**
- **Purpose**: Writes a formatted string to the output stream with a newline.
- **Signature**: `fn writeln(&mut self, buf: &[u8]) -> Result<(), Error>` (underlying trait method)
- **Explanation**: The `writeln!` macro extends `write!` by appending a newline (`\n`). It’s the preferred method for contest output, ensuring each result is on a new line.
- **Usage in Code**:
  ```rust
  writeln!(stdout, "{}", a + b).expect("Failed to write");
  ```
  - Writes the sum with a newline, with `expect` for error handling.

#### 12. **`BufWriter::new`**
- **Purpose**: Creates a new buffered writer with a default buffer size.
- **Signature**: `pub fn new(inner: W) -> BufWriter<W>`
- **Explanation**: This method, from the `std::io::BufWriter` struct, wraps a `Write` implementor (e.g., `StdoutLock`) to buffer output, reducing the number of write system calls and improving performance for large outputs.
- **Usage in Code**:
  ```rust
  let mut stdout = BufWriter::new(stdout.lock());
  ```
  - Buffers the locked `stdout`, with `mut` for writing.

#### 13. **`Iterator::map`**
- **Purpose**: Transforms each item of an iterator into a new form.
- **Signature**: `fn map<B, F>(self, f: F) -> Map<Self, F> where F: FnMut(Self::Item) -> B`
- **Explanation**: This method applies a closure to each element, producing a new iterator. It’s used here to parse strings into integers.
- **Usage in Code**:
  ```rust
  let numbers: Vec<i32> = input.trim().split_whitespace().map(|s| s.parse().expect("Expected an integer")).collect();
  ```
  - Maps each token to an `i32` via `parse`.

#### 14. **`Iterator::collect`**
- **Purpose**: Collects the items of an iterator into a collection (e.g., `Vec`).
- **Signature**: `fn collect<B: FromIterator<Self::Item>>(self) -> B`
- **Explanation**: This method consumes the iterator and builds a collection (e.g., `Vec<i32>`) from its items. The type annotation (`Vec<i32>`) specifies the target collection.
- **Usage in Code**:
  ```rust
  let numbers: Vec<i32> = input.trim().split_whitespace().map(|s| s.parse().expect("Expected an integer")).collect();
  ```
  - Collects parsed integers into a `Vec<i32>`.

#### 15. **`Iterator::sum`**
- **Purpose**: Computes the sum of all items in an iterator.
- **Signature**: `fn sum<S>(self) -> S where Self: Iterator<Item = S>, S: Sum`
- **Explanation**: This method reduces the iterator to a single value by summing its elements, requiring the item type to implement `Sum` (e.g., `i32`).
- **Usage in Code**:
  ```rust
  let result = numbers.iter().sum::<i32>();
  ```
  - Sums the elements of `numbers`, with the type annotation `<i32>` for clarity.

#### 16. **`File::open`**
- **Purpose**: Opens a file in read-only mode.
- **Signature**: `pub fn open<P: AsRef<Path>>(path: P) -> Result<File, Error>`
- **Explanation**: This method, from `std::fs::File`, returns a `File` handle for reading. The `expect` call unwraps the `Result`, assuming the file exists.
- **Usage in Code**:
  ```rust
  let input_file = File::open("input.txt").expect("Failed to open input.txt");
  ```
  - Opens `input.txt` for reading.

#### 17. **`File::create`**
- **Purpose**: Creates a new file or truncates an existing one for writing.
- **Signature**: `pub fn create<P: AsRef<Path>>(path: P) -> Result<File, Error>`
- **Explanation**: This method returns a `File` handle for writing. The `expect` call handles potential errors.
- **Usage in Code**:
  ```rust
  let output_file = File::create("output.txt").expect("Failed to create output.txt");
  ```
  - Creates or truncates `output.txt` for writing.

#### 18. **`BufReader::new`**
- **Purpose**: Creates a new buffered reader with a default buffer size.
- **Signature**: `pub fn new(inner: R) -> BufReader<R>`
- **Explanation**: This method wraps a `Read` implementor (e.g., `File`) with a `BufReader` for efficient reading, reducing system calls.
- **Usage in Code**:
  ```rust
  let mut input = BufReader::new(input_file);
  ```
  - Buffers the `input_file` for reading.

#### 19. **`lines` (via `BufRead::lines`)**
- **Purpose**: Returns an iterator over the lines of the input stream.
- **Signature**: `fn lines(self) -> Lines<Self> where Self: Sized`
- **Explanation**: This method, from `BufRead`, produces an iterator of `Result<String, Error>` for each line until EOF. It’s useful for multi-line input.
- **Usage in Code**:
  ```rust
  let mut lines = io::stdin().lock().lines();
  ```
  - Creates an iterator over input lines.

#### 20. **`Option::unwrap`**
- **Purpose**: Unwraps an `Option`, panicking if it’s `None`.
- **Signature**: `pub fn unwrap(self) -> Self::T`
- **Explanation**: This method extracts the value from a `Some` variant, assuming it exists. It’s used here with `next()` to access tokens, relying on expected input.
- **Usage in Code**:
  ```rust
  let first_line = lines.next().unwrap().expect("Failed to read line");
  ```
  - Unwraps the next line, with a second `expect` for the `Result`.

#### 21. **`Result::expect`**
- **Purpose**: Unwraps a `Result`, panicking with a custom message if it’s `Err`.
- **Signature**: `pub fn expect(self, msg: &str) -> Self::T`
- **Explanation**: This method extracts the `Ok` value, providing a message for debugging if an error occurs. It’s used extensively to handle I/O and parsing errors.
- **Usage in Code**:
  ```rust
  stdin.read_line(&mut input).expect("Failed to read line");
  ```
  - Ensures the read operation succeeds or panics with the message.

---

### Summary of Methods by Category

- **Input Handling**:
  - `io::stdin`, `Stdin::lock`, `BufRead::read_line`, `BufRead::lines`, `str::trim`, `str::split_whitespace`, `Iterator::next`, `str::parse`, `Iterator::collect`
- **Output Handling**:
  - `io::stdout`, `Stdout::lock`, `Write::write` (via `write!`), `Write::writeln` (via `writeln!`), `BufWriter::new`
- **File I/O**:
  - `File::open`, `File::create`, `BufReader::new`
- **Iterator Operations**:
  - `Iterator::map`, `Iterator::sum`
- **Error Handling**:
  - `Option::unwrap`, `Result::expect`

### Notes
- **Safety**: Methods like `unwrap` and `expect` are used assuming valid contest input. In production, consider `match` or `?` for proper error handling.
- **Performance**: Buffered I/O (`lock`, `BufReader`, `BufWriter`) and reusable buffers optimize for speed, critical in contests.
- **Flexibility**: These methods support a range of input formats (single line, multi-line, file-based), making them versatile for competitive programming.

If you need deeper insights into any method or examples of their use in specific problems, let me know!