Of course. Here is the HTML base code generated from the transcript.

HTML

```HTML
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Accessing Elements</title>
    <style>
        html {
            font-family: sans-serif;
        }

        body {
            max-width: 500px;
            margin: 0 auto;
        }

        div, p {
            padding: 0.8em;
            margin: 0.8em 0;
            border: 1px solid grey;
        }
    </style>
</head>
<body>

    <h1 id="main">Accessing Elements</h1>

    <h2>Using the ID (#header)</h2>
    <div id="header">
        Access this by using <strong>getElementById()</strong>.
    </div>

    <h2>Using the Class Name (.header)</h2>
    <div class="header">
        Access this by using <strong>getElementsByClassName()</strong>.
    </div>
    <div class="header">
        2
    </div>

    <h2>Using the Tag (&lt;p&gt;)</h2>
    <p>
        Access this by using <strong>getElementsByTagName()</strong>.
    </p>
    <p>
        Access this by using <strong>getElementsByTagName()</strong>.
    </p>

    <h2>Using the querySelector() with id of "query"</h2>
    <div id="query">
        Access this by using <strong>querySelector()</strong>.
    </div>
    
    <h2>Using the querySelectorAll() with class of "query-all"</h2>
    <div class="query-all">
        Access this by using <strong>querySelectorAll()</strong>.
    </div>
    <div class="query-all">
        Access this by using <strong>querySelectorAll()</strong>.
    </div>

</body>
</html>
```