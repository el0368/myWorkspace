This is your **Master Plan**. We will build your knowledge layer by layer. We do not jump to "Phase 3" until you can survive "Phase 1" without crashing.

We will divide the course into **Four Distinct Phases**. Each phase combines **Zig Syntax** (The Tools) with **DSA Concepts** (The Logic).

---

### **Phase 1: The Safe Zone (Stack & Static Memory)**

**Goal:** Master logic and fixed data. No manual memory management yet. Everything lives on the Stack.

|**Lesson**|**Zig Syntax (The Tool)**|**DSA Concept (The Logic)**|**The "Trap" to Avoid**|
|---|---|---|---|
|**1.1**|`const` vs `var`, Primitives (`u8`, `i32`, `bool`)|**Space Complexity:** Bits, Bytes, and Integer Overflow.|Choosing `i32` for a value that is never negative.|
|**1.2**|Arrays `[N]T`, `if`/`else`|**Random Access:** $O(1)$ access time vs. Linear Scan.|Index Out of Bounds errors.|
|**1.3**|Loops `for`, `while`, Range `0..10`|**Iteration:** Linear Search $O(N)$.|Infinite loops.|
|**1.4**|Functions `fn`|**The Call Stack:** How the machine "remembers" where to go back.|Stack Overflow (Infinite Recursion).|
|**1.5**|`struct` (Basic)|**Data Packing:** Grouping related data (Records).|creating disorganized "spaghetti" variables.|

> **Checkpoint Project:** A "Grade Calculator" that stores fixed scores, calculates averages, and assigns letter grades.

---

### **Phase 2: The Bridge (Pointers & Views)**

**Goal:** Learn to reference data without copying it. This is the hardest conceptual shift.

|**Lesson**|**Zig Syntax (The Tool)**|**DSA Concept (The Logic)**|**The "Trap" to Avoid**|
|---|---|---|---|
|**2.1**|Address-of `&` and Pointers `*T`|**Indirection:** The difference between the "Map" and the "Territory".|Dereferencing `null` or bad addresses.|
|**2.2**|Dereference `.*`|**Pass-by-Reference:** Modifying data inside other functions.|Modifying data you shouldn't touch.|
|**2.3**|Slices `[]T`|**Views:** Looking at a subset of an array without copying.|Accessing a slice after the array is gone.|
|**2.4**|Strings (as `[]u8`)|**Immutable Data:** Why strings are just bytes.|Trying to edit a string literal.|

> **Checkpoint Project:** A text analyzer that counts specific words in a sentence without creating new strings.

---

### **Phase 3: The Wild (Heap & Dynamic Memory)**

**Goal:** Managing memory manually. This is where we revisit the "Manager" (Allocator) concepts.

|**Lesson**|**Zig Syntax (The Tool)**|**DSA Concept (The Logic)**|**The "Trap" to Avoid**|
|---|---|---|---|
|**3.1**|`allocator`, `defer`, `deinit`|**The Heap:** Dynamic allocation vs. Static Stack.|**Memory Leaks.**|
|**3.2**|Error Handling `!void`, `try`|**Fault Tolerance:** Handling "Out of Memory" gracefully.|Ignoring errors (Using `catch unreachable`).|
|**3.3**|`std.ArrayList`|**Dynamic Arrays:** Amortized Growth ($O(1)$ vs $O(N)$ insertion).|Pointer invalidation on resize.|
|**3.4**|`alloc` vs `create`|**Memory Layout:** Heap fragmentation.|Freeing memory twice (Double Free).|

> **Checkpoint Project:** A simple "Note Taking" app that can accept an unlimited number of notes.

---

### **Phase 4: The Chains (Nodes & Links)**

**Goal:** Building your own Data Structures from scratch.

|**Lesson**|**Zig Syntax (The Tool)**|**DSA Concept (The Logic)**|**The "Trap" to Avoid**|
|---|---|---|---|
|**4.1**|Self-referential Structs `?*Node`|**Linked Lists:** Nodes pointing to Nodes.|Losing the "Head" pointer (Data loss).|
|**4.2**|Heap-allocated Nodes|**Insertion/Deletion:** $O(1)$ structural changes.|Dangling pointers.|
|**4.3**|Recursion with Pointers|**Trees:** Binary Search Trees.|Infinite loops in circular data.|
|**4.4**|`std.StringHashMap`|**Hashing:** Key-Value pairs and collisions.|Slow lookups due to bad keys.|

> **Final Boss Project:** A complete "Task Manager" using Linked Lists to order tasks by priority.

---

## Where We Start: Phase 1, Lesson 1.1

We are starting fresh. Forget the Allocators for now. We are going to the **Stack**.

### Lesson 1.1: Primitives & Space

In Zig, there are no "Numbers." There are only specific containers.

**The Integer Ladder:**

- `u8`: Small (0 to 255). Perfect for: Age, Days of Month, Color Values (0-255).
    
- `u16`: Medium (0 to 65,535). Perfect for: Map coordinates, RPG Health points.
    
- `u32`: Standard (0 to 4 Billion). Perfect for: Database IDs, High Scores.
    
- `usize`: The size of your RAM (Addressable Memory). Used for counting items in a list.
    

**The Boolean:**

- `bool`: True or False. Uses 1 byte (usually).
    

### Your First Task (The Stress Test)

You are designing a **Character** struct for a video game. You need to choose the most efficient type for each attribute to save memory.

**Attributes:**

1. **Level:** (Max level is 100).
    
2. **Gold:** (Can be up to 10,000,000).
    
3. **IsAlive:** (Yes or No).
    
4. **XP:** (Experience points, can never be negative).
    

**Write the code defining these variables using `const` or `var` and the correct Type.** _(Hint: Think about which ones change and which ones don't)._